<h2>{{players.length}} players</h2>

{{#each players as |player index|}}
<h3>
  <a href="/players/{{player.id}}">{{player.name}}</a> has scored
  {{#gt player.goals 0}}
    goals
  {{else}}
    no goals
  {{/gt}}
</h3>
{{/each}}
